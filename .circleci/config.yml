version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
      - image: cimg/node:stable
    steps:
      - checkout
      - run:
          name: Install Yarn
          command: "curl -o- -L https://yarnpkg.com/install.sh | bash"
